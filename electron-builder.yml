appId: com.tianxing.dispatch
productName: tianxing-dispatch
directories:
  output: release
  buildResources: build

# Where "electron-builder --publish" uploads the installers.
# On a v* tag, CI attaches the .exe / .dmg to a GitHub Release for tianxing-dispatch.
# releaseType "release" = published immediately and visible in the sidebar.
# Change to "draft" if you'd rather review before it goes public.
publish:
  provider: github
  owner: rakharamadhana
  repo: tianxing-dispatch
  releaseType: release
files:
  - out/**
  - package.json
# better-sqlite3 is a native module; ship + rebuild it for each target.
npmRebuild: true
asarUnpack:
  - "**/node_modules/better-sqlite3/**"

win:
  target:
    - nsis
  # Stable (version-less) name so /releases/latest/download links never break.
  artifactName: ${productName}-setup.${ext}

nsis:
  oneClick: false
  perMachine: false
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true
  createStartMenuShortcut: true

mac:
  target:
    - dmg
  category: public.app-category.business
  # Stable (version-less) name so /releases/latest/download links never break.
  artifactName: ${productName}.${ext}

linux:
  target:
    - AppImage
  category: Office
