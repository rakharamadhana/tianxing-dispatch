export const BRANCH_OPTIONS = ['台北', '新竹', '高雄']
// Pseudo-branch tab: the CEO's combined view across all branches (see
// src/main/branchMap.js#ALL_LABEL, which this must match).
export const ALL_LABEL = '全部'
